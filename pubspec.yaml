name: daily_adhkar_tracker
description: Daily Adhkar Tracker — hadith, daily counter, local history

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  shared_preferences: ^2.2.3
  google_fonts: ^6.3.2
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
